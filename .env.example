# NVIDIA NIM API key (server-side only — never expose to the browser)
NVIDIA_API_KEY=

# Optional override. Defaults to llama-4-maverick instruct model.
NVIDIA_MODEL=meta/llama-4-maverick-17b-128e-instruct
